<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sun.alone.demo_databinding">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Demo_DataBinding">
        <activity
            android:name=".ui.DoubleBindingActivity"
            android:exported="false" />
        <activity
            android:name=".ui.ObservableActivity"
            android:exported="false" />
        <activity
            android:name=".ui.LayoutDetailActivity"
            android:exported="false" />
        <activity
            android:name=".ui.CustomAttributeActivity"
            android:exported="false" />
        <activity android:name=".ui.BaseUseActivity" />
        <activity android:name=".ui.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>